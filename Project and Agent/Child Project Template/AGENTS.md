# 项目级 AGENTS.md

本文件叠加 guofu 的全局 `/Users/guofu/.codex/AGENTS.md`。

若与全局规则冲突，以本项目更具体规则为准。

本文件是项目级主入口，目标是轻量、清楚、可路由：保留每次都需要执行的关键调用，把长流程放到 `docs/agent/` 按需读取。

## 1. 项目定位

一句话定位：

> 待补充。

目标用户：

- 待补充。

当前阶段：

- 待补充。

最重要的成功标准：

- 待补充。

当前不做：

- 待补充。

## 2. 默认读序

开始本项目任务时，默认按以下顺序理解上下文：

1. 用户当前消息。
2. 全局 `/Users/guofu/.codex/AGENTS.md`。
3. 本项目 `AGENTS.md`。
4. 本项目 README、核心需求文档、设计规范、测试说明或数据说明。
5. 当前任务直接相关的代码、数据、配置、运行结果。
6. 命中分支场景时，再读取 `docs/agent/` 或 `.learnings/`。

原则：

- 用户最新明确指令优先。
- 本项目规则优先于全局通用规则。
- 代码、数据、测试、运行结果优先于推测。
- 不默认整篇读取大型历史资料。

## 3. 核心资料入口

本项目的核心资料入口：

- `README.md`：项目总入口。没有时先忽略。
- `docs/`：需求、设计、测试、数据口径和交付说明。没有时先忽略。
- `docs/agent/workflows.md`：复杂任务、需求澄清、原型、实施计划和交付复盘的工作流说明。
- `docs/agent/memory-and-decisions.md`：主动沉淀、决策记录、明确错误与待确认口径的处理规则。
- `.learnings/LEARNINGS.md`：项目已确认经验。只有涉及项目延续、历史规则或沉淀候选时读取。
- `.learnings/ERRORS.md`：项目已踩错误。只有排查失败、重复错误或环境问题时读取。

如本项目有更具体的权威文档，在这里补充：

- 待补充。

## 4. 分支规则调用

只在命中场景时读取分支文件。

读取 `docs/agent/workflows.md` 的场景：

- 需求模糊、陌生模块、新功能或影响范围较大。
- 需要 Blind Spot Pass、Reverse Interview、Prototype First、Implementation Plan、Implementation Notes、Delivery Review 或 Final Exam。
- 涉及架构、数据、权限、核心流程、重要体验路径或多文件正式改动。

读取 `docs/agent/memory-and-decisions.md` 的场景：

- 用户表达后续要遵守的项目规则、术语、字段、流程、偏好或边界。
- 对话中出现可复用经验、重复错误、稳定验证方式或明确项目裁定。
- 需要判断内容应写入项目 `AGENTS.md`、`.learnings/`、`docs/decision-log.md` 还是全局规则。
- 需要区分明确错误与待确认口径。

## 5. 执行原则

- 简单、低风险、目标明确的任务直接执行。
- 复杂、模糊、高风险任务先澄清目标、约束、风险和验证方式。
- 优先选择最小必要方案。
- 只改当前任务直接相关内容。
- 不顺手重构无关代码。
- 不引入不必要的新依赖。
- 不为了未来不确定需求提前设计复杂架构。
- 保持项目现有风格、目录习惯、命名方式和验证方式。
- 发现无关问题可以指出，但不要擅自一起改。

## 6. 风险边界

遇到以下情况，先说明风险并等待确认：

- 修改数据模型、权限模型、接口合约或核心流程。
- 删除文件、删除数据、覆盖用户已有内容。
- 修改部署、密钥、环境配置或线上环境。
- 大范围格式化、大范围重构或重写核心模块。
- 业务口径不明确，且不同选择会导致不同实现。
- 发现历史实现或文档存在明确错误，但修复会扩大影响范围。

可以自行处理的低风险事项：

- 明确拼写错误。
- 类型错误。
- 局部 lint 问题。
- 不改变业务逻辑的局部整理。
- 为已有逻辑补充测试。
- 按项目既有规范补齐简单状态或样式。

前提：不改变业务口径、数据模型、接口定义、权限、历史兼容，不引入新依赖。

## 7. 验证要求

交付前必须说明验证到什么程度。

默认验证顺序：

1. 运行项目已有的最小验证命令。
2. 若没有统一验证命令，运行与本次改动直接相关的检查。
3. 若验证无法运行，说明原因、风险和替代检查。

本项目常用验证命令：

```bash
# 待补充
```

不要只说“已完成”，要说明：

- 改了什么。
- 为什么这样改。
- 怎么验证。
- 哪些风险仍存在。

## 8. 文档与沉淀

如果本次改动改变了需求、字段、状态、接口、权限、页面行为、数据口径或验证方式，应同步更新对应文档。

Agent 应主动识别可沉淀内容，不等待用户专门要求。具体判断和写入规则见 `docs/agent/memory-and-decisions.md`。

默认规则：

- 项目固定规则优先写入项目 `AGENTS.md`。
- 项目经验、业务口径、命名和后续复用事实写入 `.learnings/LEARNINGS.md`。
- 已踩错误、失败原因和避免方式写入 `.learnings/ERRORS.md`。
- 重要长期决策按需写入 `docs/decision-log.md`。
- 跨项目规则先提示用户确认，再考虑更新全局 `/Users/guofu/.codex/AGENTS.md`。

用户可以随时要求本轮暂停沉淀、只列候选、不写入文件。

## 9. 项目输出风格

本项目专属输出要求：

- 在整个对话的最终回复结束之后，追加：`以上~`

如果本项目有固定术语、章节命名、交付格式，在这里补充。

## 10. 推荐项目文档结构

推荐结构：

```text
/
├── AGENTS.md
├── README.md
├── docs/
│   ├── agent/
│   │   ├── workflows.md
│   │   └── memory-and-decisions.md
│   ├── implementation-plan.md
│   ├── implementation-notes.md
│   ├── unknowns-log.md
│   ├── decision-log.md
│   └── final-report.md
└── .learnings/
    ├── LEARNINGS.md
    └── ERRORS.md
```

不强制每个项目一次性建齐。`docs/agent/` 和 `.learnings/` 随模板创建；其他任务触发型文件需要时再创建。

## 11. 本文件维护规则

本文件优先保持短入口和清楚路由。

适合写在本文件里的内容：

- 项目定位。
- 权威资料入口。
- 固定读序。
- 分支规则调用条件。
- 风险边界。
- 常用验证命令。
- 项目专属输出风格。

不适合写在本文件里的内容：

- 大段需求正文。
- 大段设计稿说明。
- 大段数据字典。
- 一次性任务计划。
- 频繁变化的中间结论。
- 复杂工作流全文。
- 沉淀和决策机制细则。

这些内容应放在 `docs/`、`docs/agent/` 或 `.learnings/`，并在本文件中给出清楚入口。
