# Case: GPT-5.6 Agent Refinement

## 目标

确认全局和项目级 `AGENTS.md` 按 GPT-5.6 提示词特性收敛为 outcome-first、证据驱动、少重复、可停止的规则。

## 模拟输入

用户说：基于 GPT-5.6 官方提示词建议，优化全局和项目级 Agent 规则。

## 预期触发

- 全局规则集中表达执行授权。
- 全局规则补充证据边界。
- 全局规则补充完成与停止条件。
- 技能触发改成最小技能集合，不因一次遗漏自动修改规则。
- 产品和商业判断只在会影响方案时输出。
- 项目级模板收敛为项目结果、权威来源、项目约束、工具路由、验证完成、输出沉淀六段。
- 项目级沉淀先识别候选，写入服从当前任务授权。

## 验证点

- 全局 `AGENTS.md` 包含执行授权、证据边界、完成与停止条件。
- 全局 `AGENTS.md` 不再保留自动规则自修复和固定七段式输出。
- 项目模板不重复全局读序和执行原则。
- 项目模板不默认追加项目集结束语。
- 推荐目录和模板维护规则移动到项目模板 README。
